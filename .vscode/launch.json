{
  "version": "0.2.0",
  "configurations": [
    // Android debugging requires ADB + physical/emulator device
    // Use: adb logcat | grep FishIT
    // Kotlin debugger extension is disabled for memory reasons
  ]
}