# Telegram Reference Documentation

This directory contains organized Telegram reference files.

## Directory Structure

- `cli/` - TDLib CLI source files (extracted from `tdl_cli_source_only.zip`)
- `exports/` - TDLib export files (extracted from `tdl_exports.zip`)

## Status

**Pending**: The ZIP files have not been uploaded yet. Once the following files are uploaded to `/docs`:
- `tdl_cli_source_only.zip`
- `tdl_exports.zip`

They should be extracted into their respective directories.
