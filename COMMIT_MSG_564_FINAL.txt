fix(#564): True compile-time gating for Chucker & LeakCanary

BREAKING CHANGE: Debug tools now use reflection-based access

When building with -PincludeChucker=false -PincludeLeakCanary=false:
- Libraries are NOT included in the APK at all
- No auto-init ContentProviders run
- Zero runtime overhead
- isAvailable returns false → UI sections hidden

Changes:
- All build.gradle.kts: Conditional dependencies with if(includeX)
- LeakDiagnosticsImpl: Complete reflection-based rewrite
- ChuckerDiagnosticsImpl: Reflection-based Chucker access
- DebugToolsInitializer: Reflection for LeakCanary config
- GatedChuckerInterceptor: Lazy reflection-based creation
- XtreamOkHttpClientProviderImpl: Reflection-based interceptor
- LeakCanaryConfig: Full reflection API
- DebugSettingsModule: Removed ChuckerInterceptor provider
- GatedChuckerInterceptorTest: Updated for new constructor

Removed all direct imports:
- import leakcanary.*
- import com.chuckerteam.chucker.*

Fixes #564
