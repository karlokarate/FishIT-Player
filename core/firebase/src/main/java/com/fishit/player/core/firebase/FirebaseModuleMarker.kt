/**
 * Core Firebase module for FishIT Player v2.
 *
 * Contains Firebase facade implementations:
 * - FirebaseFeatureFlagProvider
 * - FirebaseRemoteProfileStore
 *
 * v2 must run without Firebase by falling back to local providers.
 */
package com.fishit.player.core.firebase

/**
 * Firebase module marker.
 * TODO: Add actual Firebase implementations when needed.
 */
object FirebaseModuleMarker
