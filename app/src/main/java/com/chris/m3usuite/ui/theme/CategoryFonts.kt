package com.chris.m3usuite.ui.theme

import androidx.compose.ui.text.font.Font
import androidx.compose.ui.text.font.FontFamily
import com.chris.m3usuite.R

object CategoryFonts {
    // Valid embedded TTFs (all present in res/font)
    val Nosifer = FontFamily(Font(R.font.nosifer_regular))
    val Bangers = FontFamily(Font(R.font.bangers_regular))
    val Orbitron = FontFamily(Font(R.font.orbitron))
    val Cinzel = FontFamily(Font(R.font.cinzel))
    val Rye = FontFamily(Font(R.font.rye_regular))

    // MountainsOfChristmas not available yet → keep default to avoid crashes
    val MountainsOfChristmas = FontFamily.Default
    val Parisienne = FontFamily(Font(R.font.parisienne_regular))
    val Fredoka = FontFamily(Font(R.font.fredoka))
    val PlayfairDisplay = FontFamily(Font(R.font.playfairdisplay))
    val Merriweather = FontFamily(Font(R.font.merriweather_regular))
    val Teko = FontFamily(Font(R.font.teko))
    val AdventPro = FontFamily(Font(R.font.adventpro))
    val Baloo2 = FontFamily(Font(R.font.baloo2_regular))
    val MPlusRounded1c = FontFamily(Font(R.font.mplusrounded1c_regular))
    val YatraOne = FontFamily(Font(R.font.yatraone_regular))
    val RussoOne = FontFamily(Font(R.font.russoone_regular))
    val Oswald = FontFamily(Font(R.font.oswald))
    val Inter = FontFamily(Font(R.font.inter))
    val Staatliches = FontFamily(Font(R.font.staatliches_regular))
}
