diff --git a/app/build.gradle.kts b/app/build.gradle.kts
index 9831d2a..47d3f2c 100644
--- a/app/build.gradle.kts
+++ b/app/build.gradle.kts
@@ -128,14 +128,14 @@ android {
     }
     composeOptions { kotlinCompilerExtensionVersion = "1.5.14" }
 
-    // >>> Angepasst: Split-APKs pro ABI + Universal-APK
+    // >>> Angepasst: Split-APKs pro ABI (Universal-APK deaktiviert)
     splits {
         abi {
             isEnable = true
             reset()
             include("armeabi-v7a", "arm64-v8a")
-            // Zusätzlich zur je-ABI APK eine Universal-APK erzeugen
-            isUniversalApk = true
+            // Keine Universal-APK, nur per-ABI (siehe Architektur-Docs)
+            isUniversalApk = false
         }
     }
 
